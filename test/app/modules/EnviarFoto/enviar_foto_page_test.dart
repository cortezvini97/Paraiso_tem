import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:vcid/app/modules/EnviarFoto/enviar_foto_page.dart';

void main() {
  testWidgets('EnviarFotoPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(EnviarFotoPage(title: 'EnviarFoto')));
    //  final titleFinder = find.text('EnviarFoto');
    //  expect(titleFinder, findsOneWidget);
  });
}
