import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:vcid/app/modules/Pagina/pagina_page.dart';

void main() {
  testWidgets('PaginaPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(PaginaPage(title: 'Pagina')));
    //  final titleFinder = find.text('Pagina');
    //  expect(titleFinder, findsOneWidget);
  });
}
