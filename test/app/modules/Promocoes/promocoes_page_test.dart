import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:vcid/app/modules/Promocoes/promocoes_page.dart';

void main() {
  testWidgets('PromocoesPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(PromocoesPage(title: 'Promocoes')));
    //  final titleFinder = find.text('Promocoes');
    //  expect(titleFinder, findsOneWidget);
  });
}
