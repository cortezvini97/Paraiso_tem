import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:vcid/app/modules/WebView/web_view_page.dart';

void main() {
  testWidgets('WebViewPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(WebViewPage(title: 'WebView')));
    //  final titleFinder = find.text('WebView');
    //  expect(titleFinder, findsOneWidget);
  });
}
