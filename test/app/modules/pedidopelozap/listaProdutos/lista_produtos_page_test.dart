import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:vcid/app/modules/pedidopelozap/listaProdutos/lista_produtos_page.dart';

void main() {
  testWidgets('ListaProdutosPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(ListaProdutosPage(title: 'ListaProdutos')));
    //  final titleFinder = find.text('ListaProdutos');
    //  expect(titleFinder, findsOneWidget);
  });
}
